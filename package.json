{
  "name": "physio-prime",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "admin",
    "src"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w src\" \"npm run dev -w admin\"",
    "build": "npm run build -w server && npm run build -w src && npm run build -w admin",
    "lint": "npm run lint -w server && npm run lint -w src && npm run lint -w admin",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
